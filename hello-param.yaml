apiVersion: tekton.dev/v1alpha1
kind: StepAction
metadata:
  name: hello-param
spec:
  params:
    - name: param1
      description: The first parameter
      type: string
      default: "/foo/bar"
  results:
    - name: result1
      description: The first result
      type: string
  image: alpine
  script: |
    echo -n hello HARDCODE | tee $(results.result1.path)
